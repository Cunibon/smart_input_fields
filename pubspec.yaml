name: smart_input_fields
description: "A input field that auto evaluates math expressions into numbers."
version: 0.0.1
repository: https://github.com/Cunibon/smart_input_fields

environment:
  sdk: ^3.12.0
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  math_expressions: ^3.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

